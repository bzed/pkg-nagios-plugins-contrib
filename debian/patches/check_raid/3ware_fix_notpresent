diff --git a/check_raid/check_raid b/check_raid/check_raid
index fc24761..aee5634 100644
--- a/check_raid/check_raid
+++ b/check_raid/check_raid
@@ -761,7 +761,7 @@ sub check_tw_cli {
 			next unless (my($p, $s,) = /^(p\d+)\s+(\S+)\s+.+\s+.+\s+.+/);
 			push(@ds, "$p:$s");
 			foreach (@ds) {
-				$status = $ERRORS{CRITICAL} unless (/p\d+:OK/);
+				$status = $ERRORS{CRITICAL} unless (/p\d+:(OK|NOT-PRESENT)/);
 			}
 		}
 
